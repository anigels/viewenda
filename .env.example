# Server-only TMDB read access token. Never prefix this with VITE_.
TMDB_BEARER_TOKEN=your_tmdb_read_access_token_here

# Public HTTPS origin of the hosted Viewenda backend, required for mobile builds.
VITE_API_ORIGIN=
